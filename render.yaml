services:
  - type: web
    name: dealhunter-server
    env: node
    buildCommand: npm install && cd react/dealhunter-client && npm ci && npm run build && cd ../..
    startCommand: npm start
    envVars:
      - key: PORT
        generateValue: true
      - key: NODE_ENV
        value: production

