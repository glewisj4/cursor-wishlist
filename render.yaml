services:
  - type: web
    name: dealhunter-server
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        generateValue: true
      - key: NODE_ENV
        value: production

