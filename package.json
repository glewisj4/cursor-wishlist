{
  "name": "dealhunter-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd react/dealhunter-client && npm ci && npm run build",
    "build:react": "cd react/dealhunter-client && npm ci && npm run build",
    "install:chrome": "node install-chrome.js",
    "start": "node server.js",
    "dev": "node server.js",
    "postinstall": "npm run install:chrome && cd react/dealhunter-client && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cheerio": "^1.1.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "puppeteer": "^24.33.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
